# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

ttt-expert-v1 — Tic-Tac-Toe expert system (v1).

## External Integrations

- **GitLab MCP server** is configured in `.claude/.mcp.json` for interacting with the GitLab API (issues, merge requests, etc.) at `gitlab.noveogroup.com`.
